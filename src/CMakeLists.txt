INCLUDE_DIRECTORIES (
  "${CMAKE_SOURCE_DIR}"
  "${CMAKE_CURRENT_SOURCE_DIR}"
)

FILE(GLOB_RECURSE mf_SRC "mf/*.cpp")

ADD_LIBRARY(no_mf ${mf_SRC})

INSTALL(TARGETS
  no_mf
  LIBRARY DESTINATION lib
  ARCHIVE DESTINATION lib
)
